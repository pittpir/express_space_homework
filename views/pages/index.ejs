<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf8">
	<title>EJS Example</title>
</head>
<body>
	<h1>Missions:</h1>
	
	<ul class="missions"> <% data.map((item) => { %>
		<li><a href="localhost:3000/missions/<%= item.name %>" ><%= item.name %></li>
		<% }); %>
	</ul>
</body>
</html>

